// Pointer Type Mismatch

#include <bits/stdc++.h>

using namespace std;



int main()
{
    // YOUR_COMMENT: Write your explanation - The type of the value a pointer points to must be the same. For example if the variable a is an integer, so our pointer must also be of type integer. So the variable c is of type char, but the pointer is type of double. Therefore, even without running the code, the compiler can already detect this error.
    

    return 0;
}
